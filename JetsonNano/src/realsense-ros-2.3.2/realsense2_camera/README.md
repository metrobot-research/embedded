# launch
+ rs_d435_camera_with_model.launch
  + depth & color both:
    + 1280*720
    + 30 fps

# Filters
+ should be on
  + decimation
  + spatial
  + temporal
+ in `base_realsense_node`
  + _filters_str
  + _pointcloud

